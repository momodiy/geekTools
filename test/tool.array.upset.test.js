const upsetArr = require('../tool/array/tool.array.upset');
let a = [1, 2, 3, 4, 5, 6, 7];
console.log(666);
console.log(a.upset().toString() === a.toString() ? 'through the test' : 'test to fail');

